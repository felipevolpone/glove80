# Mouse keys (&mkp / &mmv / &msc) used by the MOUSE layer.
CONFIG_ZMK_POINTING=y

# Deep sleep after 15 min of inactivity (same as the Charybdis config).
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=900000
